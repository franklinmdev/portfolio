---
import Layout from "@/layouts/Layout.astro"
import CustomLoader from "@/components/CustomLoader.astro"
---

<Layout title="Loader Test - Franklin Martinez">
  <div class="min-h-screen bg-[#111111] py-24">
    <div class="container mx-auto max-w-4xl px-4">
      <div class="mb-12 text-center">
        <h1 class="mb-4 text-4xl font-bold text-white">Loader Test Page</h1>
        <p class="text-lg text-zinc-400">
          Test the custom loaders in different scenarios
        </p>
      </div>

      <div class="grid gap-8 md:gap-12">
        <div class="rounded-lg border border-zinc-700 bg-zinc-900 p-6">
          <h2 class="mb-4 text-xl font-semibold text-white">
            Static Loader Examples
          </h2>
          <div class="flex items-center justify-center gap-8 py-8">
            <div class="text-center">
              <CustomLoader size="sm" class="mx-auto mb-2" />
              <p class="text-sm text-zinc-400">Small</p>
            </div>
            <div class="text-center">
              <CustomLoader size="md" class="mx-auto mb-2" />
              <p class="text-sm text-zinc-400">Medium</p>
            </div>
            <div class="text-center">
              <CustomLoader size="lg" class="mx-auto mb-2" />
              <p class="text-sm text-zinc-400">Large</p>
            </div>
          </div>
        </div>

        <div class="rounded-lg border border-zinc-700 bg-zinc-900 p-6">
          <h2 class="mb-4 text-xl font-semibold text-white">
            Navigation Tests
          </h2>
          <p class="mb-4 text-zinc-400">
            These links will trigger the page loader:
          </p>
          <div class="flex flex-wrap gap-4">
            <a
              href="/en"
              class="inline-flex items-center rounded-md bg-violet-600 px-4 py-2 text-white transition-colors hover:bg-violet-700"
            >
              Go to Home (EN)
            </a>
            <a
              href="/es"
              class="inline-flex items-center rounded-md bg-violet-600 px-4 py-2 text-white transition-colors hover:bg-violet-700"
            >
              Go to Home (ES)
            </a>
            <a
              href="/en#projects"
              class="inline-flex items-center rounded-md bg-zinc-700 px-4 py-2 text-white transition-colors hover:bg-zinc-600"
            >
              Go to Projects Section
            </a>
          </div>
        </div>

        <div class="rounded-lg border border-zinc-700 bg-zinc-900 p-6">
          <h2 class="mb-4 text-xl font-semibold text-white">
            Language Switch Test
          </h2>
          <p class="mb-4 text-zinc-400">
            Use the language switcher in the header (EN/ES) to see the loader in
            action.
          </p>
          <div class="rounded-md bg-zinc-800 p-4">
            <p class="text-sm text-zinc-300">
              <strong>Note:</strong> The page loader will appear for ~300-500ms when:
            </p>
            <ul
              class="mt-2 list-inside list-disc space-y-1 text-sm text-zinc-400"
            >
              <li>Clicking the language switcher (EN/ES)</li>
              <li>Navigating to different pages</li>
              <li>Using browser back/forward buttons</li>
              <li>Page reloads (briefly)</li>
            </ul>
          </div>
        </div>

        <div class="rounded-lg border border-zinc-700 bg-zinc-900 p-6">
          <h2 class="mb-4 text-xl font-semibold text-white">Reload Test</h2>
          <p class="mb-4 text-zinc-400">
            Click this button to reload the page and see the loader:
          </p>
          <button
            onclick="window.location.reload()"
            class="inline-flex items-center rounded-md bg-red-600 px-4 py-2 text-white transition-colors hover:bg-red-700"
          >
            Reload Page
          </button>
        </div>

        <div class="text-center">
          <a
            href="/en"
            class="inline-flex items-center rounded-md bg-gradient-to-r from-violet-600 to-purple-600 px-6 py-3 text-white transition-all hover:from-violet-700 hover:to-purple-700"
          >
            ← Back to Portfolio
          </a>
        </div>
      </div>
    </div>
  </div>
</Layout>
